# DO NOT ADD SECRETS TO THIS FILE. This is a good place for defaults.
# If you want to add secrets use `.env.production.local` instead, which is automatically detected by docker-compose.
REACT_APP_API_ENDPOINT=http://localhost:3000/api/v1/

# header | cookie
REACT_APP_AUTH_METHOD=header

AUTH_SECRET= # Linux: `openssl rand -base64 32` or go to https://generate-secret.vercel.app/32
